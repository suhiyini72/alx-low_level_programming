#include <stdio.h>

/**
 * This prints alphabets in lowercase fallowed by a new line character
 * Return: alphabets in lowercase
 */
int main(){
char k;
for (k = 'a'; k <= 'z'; k++){
putchar(k);
putchar('\n');
}
}
